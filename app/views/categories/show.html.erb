<h2><%= @category.name %></h2>
<hr>

<h3>Popular Reads</h3>
<hr>

<h3>New Releases</h3>
<hr>

<h3>All</h3>
<hr>
<% @books.each do |book| %>
  <div class="book">
    <%= link_to image_tag(book.image_url), book_path(book) %>
  </div>
<% end %>
